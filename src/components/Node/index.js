import { useState } from "react";
import { NodeContainer } from "./style";

const Node = ({ row, col }) => {
    const [visited, setVisited] = useState(false);

    return <NodeContainer></NodeContainer>;
};

export default Node;
